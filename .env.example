# Optional: supply to enable AI-based categorization
OPENAI_API_KEY=
# Optional: model to use, defaults to gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini
# Database URL is set via compose; override here if running locally
DATABASE_URL=sqlite:///./shopping.db